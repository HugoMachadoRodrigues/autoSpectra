# List models currently held in the in-memory cache

List models currently held in the in-memory cache

## Usage

``` r
list_cached_models()
```

## Value

Character vector of cache keys (`"family_id/prop"`)
