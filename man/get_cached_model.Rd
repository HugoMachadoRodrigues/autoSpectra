% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{get_cached_model}
\alias{get_cached_model}
\title{Retrieve a soilVAE model from the in-memory cache}
\usage{
get_cached_model(
  family_id,
  prop,
  model_dir = getOption("autoSpectra.model_dir", "models")
)
}
\arguments{
\item{family_id}{Family identifier (\code{"OSSL_VisNIR"} or \code{"OSSL_MIR"})}

\item{prop}{Soil property name (e.g. \code{"oc"})}

\item{model_dir}{Root directory where trained models are stored}
}
\value{
List with elements \code{model} (keras Model) and \code{scaler}
(list with \code{mean} and \code{sd})
}
\description{
On the first call for a given \code{family_id}/\code{prop} combination the
model is loaded from disk via \code{load_soilVAE()} and stored in the cache.
Subsequent calls return the cached object instantly.
}
